// Snapping and physics
export const SNAP_DISTANCE = 18;
export const GRID_SIZE = 20;

// Piece generation
export const DEFAULT_ROWS = 4;
export const DEFAULT_COLS = 3;
export const PIECE_SIZE = 120;

// Tab/Blank dimensions for curved edges
export const TAB_SIZE = 15;
export const TAB_DEPTH = 8;

// Piece states
export const PIECE_STATES = {
  IN_TRAY: "in_tray",
  DRAGGING: "dragging",
  SNAPPING: "snapping",
  LOCKED: "locked"
};

// Animation
export const ANIMATION_DURATION = 200; // ms
