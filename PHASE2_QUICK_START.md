# 🎯 Phase 2 Quick Start - User-Facing Filtering\n\n**Timeline**: Week 2 (Jan 6-12)\n**Duration**: 4-6 hours\n**Status**: Ready to Begin\n\n---\n\n## 🎯 Phase 2 Goal\n\nMake sure users **only see published content**, while drafts and archived items stay hidden.\n\n---\n\n## 🔍 3-Step Overview\n\n### Step 1: Find Query Functions (30 min)\n\nSearch for where data is fetched:\n\n```bash\ngrep -r \"getDocs.*quizzes\\|getDocs.*puzzles\\|getDocs.*stories\\|getDocs.*categories\" src/ --include=\"*.js\"\n```\n\nThis shows you which files need updates.\n\n### Step 2: Add Filtering (2-3 hours)\n\nIn each query, add `where` clauses:\n\n**Before**:\n```javascript\nconst snapshot = await getDocs(collection(db, 'quizzes'));\n```\n\n**After**:\n```javascript\nconst q = query(\n  collection(db, 'quizzes'),\n  where('status', '==', 'published'),\n  where('visibility', '==', 'public'),\n  orderBy('featured', 'desc')\n);\nconst snapshot = await getDocs(q);\n```\n\n### Step 3: Test (1-2 hours)\n\n```bash\nnpm start\n# Check each page:\n# - Quiz page shows published only\n# - Puzzle page shows published only\n# - Stories page shows published only\n# - Featured items at top\n# - Draft items hidden\n```\n\n---\n\n## 📋 Files to Update\n\nTypically:\n- `src/services/quizService.js`\n- `src/services/puzzleService.js`\n- `src/services/storyService.js`\n- `src/services/categoryService.js`\n- Any custom query functions\n- Components that fetch directly\n\n---\n\n## 💡 Code Pattern\n\n```javascript\nimport { query, where, orderBy } from 'firebase/firestore';\n\n// For user-facing pages\nconst q = query(\n  collection(db, 'quizzes'),\n  where('status', '==', 'published'),        // Only published\n  where('visibility', '!=', 'private'),      // Exclude private\n  orderBy('featured', 'desc'),               // Featured first\n  orderBy('createdDate', 'desc')             // Then newest\n);\n```\n\n---\n\n## ✅ Testing Checklist\n\n- [ ] User pages only show published items\n- [ ] Draft items hidden completely\n- [ ] Featured items appear first\n- [ ] Coming soon shows badge\n- [ ] Admin pages still show all items\n- [ ] No console errors\n- [ ] Pages load fast (<2 seconds)\n\n---\n\n## 🆘 Common Issues\n\n**Issue**: Firestore says \"index required\"\n**Solution**: Click the link in error → Firebase creates it automatically\n\n**Issue**: Items still showing drafts\n**Solution**: Check all query functions. Missed one?\n\n**Issue**: Coming soon items show normally\n**Solution**: Add ComingSoonBadge component to list\n\n---\n\n## 📖 Full Guide\n\nFor detailed walkthrough → [PHASE2_EXECUTION_GUIDE.md](PHASE2_EXECUTION_GUIDE.md)\n\n---\n\n**Ready to start?** Run the search command above to find all files that need updates!\n"